# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/ilovejson?schema=public"

# NextAuth Configuration
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"

# Required in production - your app's URL
NEXTAUTH_URL="http://localhost:3002"

# OAuth Providers (configure at least one)
# You can use one or multiple providers - just configure the ones you want to use

# Google OAuth Provider
# Create credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3002/api/auth/callback/google
# 3. For production, add your production URL as well
GOOGLE_CLIENT_ID="xxx"
GOOGLE_CLIENT_SECRET="xxx"

# GitHub OAuth Provider
# Create app at: https://github.com/settings/developers
# 1. Go to "OAuth Apps" → "New OAuth App"
# 2. Set Authorization callback URL: http://localhost:3002/api/auth/callback/github
# 3. For production, update the callback URL
GITHUB_ID="your-github-client-id"
GITHUB_SECRET="your-github-client-secret"

# Microsoft OAuth Provider (Azure AD / Entra ID)
# Create app at: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps
# 1. Go to "App registrations" → "New registration"
# 2. Add redirect URI: http://localhost:3002/api/auth/callback/azure-ad
# 3. Create a client secret in "Certificates & secrets"
# 4. Use 'common' for multi-tenant or your specific tenant ID
MICROSOFT_CLIENT_ID="your-microsoft-application-client-id"
MICROSOFT_CLIENT_SECRET="your-microsoft-client-secret"
MICROSOFT_TENANT_ID="common"

# Environment
NODE_ENV=development
